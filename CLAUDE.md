- 현재 선택된 model이 `Opus 4.8`일 경우, `codex-cli mcp`를 사용하여 계획을 제외한 모든 작업을 codex에게 작업을 위임한다
  - codex에게 작업을 위임할 경우, 복잡한 작업은 `gpt-5.5`, 간단한 작업은 `gpt-5.4-mini`에게 위임한다.
  - model options로 적혀 있는 `gpt-5.3-codex`는 무시하고 상단에 작성된 `gpt-5.5` 또는 `gpt-5.4-mini` 로 요청한다.
- 사용자가 별도 요청하지 않을 시에는 절대 커밋, 브랜치, 태그, 푸시 진행하지 않음
  - 커밋 시
    1. 일괄 커밋이 아니라 원자적 의미 단위로 커밋하되 파일에 인위적으로 손을 대지 말 것
    2. 일반적인 깃 커밋 컨벤션을 따르고, 메시지는 `{영어 카테고리} : {한글 메시지}`로 진행
